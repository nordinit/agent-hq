# TOOLS.md — Example Agent Tool Notes

## Agent HQ
- Task management API: ${AGENT_HQ_API_URL}/api/v1

## Workspace
- This workspace: /workspace

## SSH / Infrastructure
- SSH keys are available at /root/.ssh if mounted at runtime.
- Add any client-specific connection details here.
